@page
@model AccountingSystem.Pages.Reports.IncomeStatementModel
@{
    ViewData["Title"] = "Income Statement";
}

<h2>Income Statement</h2>

<form method="get" class="row g-2 mb-3">
    <div class="col-md-3">
        <label class="form-label">From</label>
        <input class="form-control" type="date" asp-for="From" />
    </div>
    <div class="col-md-3">
        <label class="form-label">To</label>
        <input class="form-control" type="date" asp-for="To" />
    </div>
    <div class="col-md-3 d-flex align-items-end">
        <button class="btn btn-primary w-100" type="submit" title="Generate income statement">
            Generate
        </button>
    </div>
</form>

<div class="mb-3 d-flex gap-2">
    <button class="btn btn-outline-secondary" type="button" onclick="window.print()" title="Print this report">
        Print
    </button>

    <form method="post" asp-page-handler="ExportCsv" class="d-inline">
        <input type="hidden" name="from" value="@Model.From?.ToString("yyyy-MM-dd")" />
        <input type="hidden" name="to" value="@Model.To?.ToString("yyyy-MM-dd")" />
        <button class="btn btn-outline-dark" type="submit" title="Download this report as CSV">
            Export CSV
        </button>
    </form>

    <form method="post" asp-page-handler="Email" class="d-inline">
        <input type="hidden" name="from" value="@Model.From?.ToString("yyyy-MM-dd")" />
        <input type="hidden" name="to" value="@Model.To?.ToString("yyyy-MM-dd")" />
        <button class="btn btn-outline-success" type="submit" title="Email this report to outbox">
            Email
        </button>
    </form>

    <a class="btn btn-outline-secondary" asp-page="./Index" title="Back to reports">
        Back
    </a>
</div>

@if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
{
    <div class="alert alert-success">@Model.StatusMessage</div>
}

@if (Model.Rows.Count == 0)
{
    <div class="alert alert-secondary">No report rows found.</div>
}
else
{
    <table class="table table-striped table-hover align-middle">
        <thead>
            <tr>
                <th>Account #</th>
                <th>Account Name</th>
                <th>Subcategory</th>
                <th class="text-end">Amount</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var r in Model.Rows)
            {
                <tr>
                    <td>@r.AccountNumber</td>
                    <td>@r.AccountName</td>
                    <td>@r.Subcategory</td>
                    <td class="text-end">@r.Amount.ToString("N2")</td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <th colspan="3" class="text-end">Net Income</th>
                <th class="text-end">@Model.NetIncome.ToString("N2")</th>
            </tr>
        </tfoot>
    </table>
}